# Dependencias mínimas del núcleo + daemon + CLI.
# Para instalar el proyecto en modo editable:  pip install -e .
# Backends de inferencia (vllm, etc.) van aparte: pip install -e ".[serve]"
typer>=0.12
rich>=13.7
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
